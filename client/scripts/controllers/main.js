import app from '../app';
/**
 * @ngdoc function
 * @name drawgameApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the drawgameApp
 */
app
  .controller('MainCtrl', function ($scope) {
    //stuff
  });
